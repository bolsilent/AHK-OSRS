/*
A simple script used to automate the login process
Usage: replace the key with whichever one you'd like, and replace
Username/Password with yours. Then tap the designated key
to be logged into your account.
*/

`::
SendInput, USERNAME{enter}PASSWORD{enter}
return
